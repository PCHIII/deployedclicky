import React from "react";
import "./Jumbotron.css";

const Jumbotron = props => (
    <div className="jumbotron text-white bg-dark">
        <h2>Click on a Girl to earn points, don't hit on the same one twice!</h2>
       
    </div>
)
export default Jumbotron;